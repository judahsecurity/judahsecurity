# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1

# Validation and settings
pydantic>=2.7.4
pydantic-settings>=2.1.0
email-validator==2.1.0

# Security
python-dotenv==1.0.0

# HTTP client for scanning
httpx==0.25.2
# openai>=1.x vendors httpx_aiohttp and needs aiohttp.SocketTimeoutError (aiohttp>=3.10).
# Stay below 3.14.0/3.14.1 (keepalive pool poisoning); 3.14.2+ is fine too.
aiohttp==3.13.5

# DNS and network discovery
dnspython==2.4.2

# SSL/TLS analysis
cryptography==41.0.7
pyOpenSSL==23.3.0

# HTML parsing (for Wappalyzer)
beautifulsoup4==4.12.2
lxml==4.9.3

# Async task processing
celery[redis]==5.3.4
redis==4.6.0

# Rate limiting
slowapi==0.1.9

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Image processing (for screenshots)
Pillow==10.1.0

# Screenshot capture in Docker/headless (Playwright Chromium)
playwright>=1.40.0

# AWS SDK (for SQS in production)
boto3==1.34.0

# IP address handling (for CIDR calculations)
# Note: ipaddress is part of stdlib in Python 3.3+, no extra install needed

# AI Agent and LLM Integration
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.28
openai>=1.50.0
anthropic>=0.34.0

# Graph Database
neo4j==5.15.0

# WebSocket support
websockets>=14,<16

# GitHub API for secret scanning
PyGithub==2.1.1

# MITRE ATT&CK data
mitreattack-python==3.0.0

# PDF Report Generation
weasyprint>=60.0
jinja2>=3.1.0

# Shared scanner core (CLI runners, findings, checks) — workers + agents
-e packages/asm_scanner_core

# Aegis Praetorium — Lictor / Censor / Augur guard layer (also installed in
# the Aegis Vanguard container; both agents share the same enforcement modules).
-e packages/aegis_praetorium

