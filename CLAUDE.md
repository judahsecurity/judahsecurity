# Judah Security ASM

Platform app: `frontend/`, `backend/`. Offensive harness: `aegis-vanguard/`.

Claude Code playbook skills live with the harness. From this repo:

```bash
cd aegis-vanguard && claude
```

Then `/aegis`. Do not load Vanguard skills against the platform app unless the
target is explicitly that checkout.
